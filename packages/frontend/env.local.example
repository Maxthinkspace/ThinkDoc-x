## Local dev configuration for the Office Add-in frontend.
##
## How to use:
## - Copy this file to `env.local` (or `.env.local`) in the same folder.
## - Fill in values, then restart `pnpm --filter frontend dev-server`.
##
## IMPORTANT:
## - `REACT_APP_DEV_API_TOKEN` must be a REAL token issued by the backend (`/api/auth/login`),
##   because the backend validates the JWT signature + session in the database.

# Enable dev auto-auth (so Playbook Library can call /api/playbooks)
REACT_APP_DEV_MODE=true

# Backend API base URL
# - Leave empty to use relative URLs (webpack proxy handles routing to backend)
# - Only set this if you need to bypass the proxy (e.g., for testing)
REACT_APP_API_BASE_URL=

# Paste a JWT from backend login response:
# curl -s http://localhost:3003/api/auth/login -H 'Content-Type: application/json' \
#   -d '{"email":"<email>","password":"<password>"}'
REACT_APP_DEV_API_TOKEN=PASTE_TOKEN_HERE


